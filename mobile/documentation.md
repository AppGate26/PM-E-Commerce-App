### product purchase flow 
User clicks "Buy Once" or "Select Payment Frequency"
    ↓
Check if user has valid auth token
    ↓
IF authenticated → Show pickup/delivery modal directly
    ↓
IF not authenticated → Navigate to signup page


