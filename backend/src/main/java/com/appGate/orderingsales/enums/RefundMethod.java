package com.appGate.orderingsales.enums;

public enum RefundMethod {
    RECEIVE_OTHER_PRODUCT,   // Customer receives a different product as replacement
    RECEIVE_SIMILAR_PRODUCT  // Customer receives the same/equivalent product as replacement
}
